{
  "name": "wireframe",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Wireframe: infer a protocol FSM from black-box agent sessions, compile a safe deterministic driver, prove it. No LLM on the learned path.",
  "workspaces": [
    "packages/core",
    "packages/cli",
    "packages/mcp",
    "bench"
  ],
  "scripts": {
    "build": "npm run build --workspace @wframe/core && npm run build --workspace @wframe/cli && npm run build --workspace @wframe/mcp",
    "test": "npm run test --workspace @wframe/core",
    "bench": "npm run bench --workspace @wframe/bench",
    "demo:mcp": "npm run demo --workspace @wframe/mcp"
  }
}
