# dependencies
node_modules/

# build output
dist/

# local env and secrets (never commit these)
.env
.env.*
!.env.example
.dev.vars

# cloudflare local state
.wrangler/

# wireframe MCP runtime state (recorded sessions / compiled drivers)
.wframe/

# logs and os files
*.log
.DS_Store
Thumbs.db

# throwaway prototype, superseded by packages/core and bench
/spike/

# the marketing site and the waitlist worker live outside this package repo
/web/
/worker/

# internal strategy notes and design mockups (not part of the public package)
/strategy/
/mockups/
