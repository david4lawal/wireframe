{
  "name": "@wframe/cli",
  "version": "1.0.0",
  "description": "The wireframe CLI: compile a protocol target into a safe deterministic driver, run a driver, and inspect a driver.",
  "type": "module",
  "bin": {
    "wireframe": "./dist/cli.js"
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "@wframe/core": "*"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
